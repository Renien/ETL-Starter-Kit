dependencies {
    compile([
            project(':etl-common'),
            project(':etl-datamodel'),
            libs.scalding_core
    ])

    providedCompile([
    ])

    testCompile([
    ])
}

hadoopJar {
    manifest {
        attributes("Main-Class": "com.twitter.scalding.Tool")
    }
}