dependencies {
    compile([
            libs.scalding_core,
            libs.scalding_avro,
            libs.scalding_commons,
            libs.hadoopCommon,
            libs.hadoopMapredClientCore
    ])

    providedCompile([
    ])

    testCompile([
    ])
}